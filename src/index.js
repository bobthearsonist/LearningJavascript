import React from "react";
import ReactDOM from "react-dom"; //eslint-disable-line

const element = <h1>Patient Zero</h1>;
console.log(element);
ReactDOM.render(element, document.getElementById("root"));
